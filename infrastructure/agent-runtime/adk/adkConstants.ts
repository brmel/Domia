// Shared ADK identifiers used by both the session assembler and the runtime
// (snapshot/restore). Kept in one place so the two stay in sync.
export const ADK_SNAPSHOT_PROVIDER = 'adk-gemini';
export const ADK_SESSION_USER_ID = 'domia';
export const ADK_AGENT_NAME = 'app_agent';
export const ADK_AGENT_DESCRIPTION =
    'Domia application-driving agent: perceives a target app, calls tools, and reports a verdict.';
