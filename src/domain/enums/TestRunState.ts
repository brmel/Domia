export enum TestRunState {
    IDLE = 'idle',
    RUNNING = 'running',
    PAUSED = 'paused',
    AWAITING_INPUT = 'awaiting_input',
    COMPLETED = 'completed',
    FAILED = 'failed',
    CANCELLED = 'cancelled'
}
